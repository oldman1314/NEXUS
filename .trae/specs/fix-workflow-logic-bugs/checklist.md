- [x] Transform 节点执行时 `input` 参数为直接上游节点的输出数据，而非 undefined
- [x] Output 节点执行时 `data` 字段为直接上游节点的输出数据，而非 undefined
- [x] `{{prev.status}}` 模板变量能正确解析为上游节点的 status 字段值
- [x] 拓扑排序执行：多输入节点（如 A→C, B→C）仅在 A 和 B 都完成后才执行 C
- [x] 保存已有工作流时不弹出命名对话框，直接更新
- [x] 保存新工作流时弹出命名对话框
- [x] 快速连续添加两个同类型节点时 ID 不碰撞
- [x] 加载其他工作流时日志和节点执行状态被清除
- [x] Start 节点不接受输入连线
- [x] 运行无 Start 节点的工作流时显示错误提示，不执行
- [x] 运行有循环的工作流时显示错误提示，不执行
- [x] 运行有孤立节点的工作流时显示警告，但仍可执行
- [x] 有未保存变更时新建/加载工作流弹出确认对话框
